on:
 push:
  branches:
   - main
jobs:
 build:
  runs-on: ubuntu-latest
  steps:
  - name: checkout code
    uses: actions/checkout@v3
  - name: check files
    run: ls
  - name: check index.html exists
    run: test -f wrongfile.html 
  - name: Build step
    run: echo "Bulding project..."
  - name: Test step
    run: echo "Testing apllicaton..."
    
    
   


